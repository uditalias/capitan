define(['react'], function(React) {

    var Footer = React.createClass({
        render: function() {
            return (
                <div>
                </div>
                );
        }
    });

    return Footer;
});